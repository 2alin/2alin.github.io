# Description
This is the repository of my personal web page. 

The main site (https://adilson.mx) is already available.

## Repository structure

- Files in the root directory (`index.html`, `main.js`, `style.css`) and the content in `data/` and `assets/` are being used to render my personal webpage (https://adilson.mx). 
- `apps` directory contains small apps that I develop while learning vanilla JavaScript.
- `learn`  directory contails small projects that I created while learning vanilla JavaScript.
